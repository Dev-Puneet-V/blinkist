import { createTheme } from '@mui/material/styles';

const tabsTheme = createTheme({
    palette: {
      primary: { 
        main: '#2CE080',
        // contrastText: '#11cb5f',
      }
    }
});

export default tabsTheme;